
public class 스택stack_heap {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

		// stack
		// 함수(매서드) 안에서 만들어지는 변수들이 사용하는 공간
		// 함수가 만들어지면 생성되었다가 함수가 반환되면 해제
		// 지역벼수라고 한다 (함수가 실행되는 동안 살아있음)
		
		// 힙
		// new에 의해서만 힙메모리 사용
		// new의 의미 메모리 할당요청, 메모리할당하고 할당된 메모리의 시작주소를 반환해줌
		// 메모리 요청: new
		// 메모리반납: 자바는 개발자가 직접 안함(가비지콜렉터가 수행)
		
		
		// c언어
		// int* ap = mallox( sizeof(int) * 4 );
		// free(ap);
		
		
	}

}
