package 스태틱객체매서드;

/*
 * 객체매서드: 객체(변수)를 사용하는 매서드, 변수를 매서드를 통해서만 사용해야함
 * 만드는 법: static이 없으면 객체매서드가 됨
 * 사용법: Class로 new해서 객체를 생성( 즉 변수를 먼저 만드는 행위를 해야함)
 * 		
 * A a = new A();
 * a.매서드1(); // 매서드1() 호출시 a의 정보가 전달됨
 * */
public class B {
	// static이 없음 >> 객체매서드, 인스턴스 매서드로 인식
	// B라는 클래스로 객체를 생성하고 사용해야함
	public void 매서드1() {
		System.out.println("매서드1 실행");
	}
}
