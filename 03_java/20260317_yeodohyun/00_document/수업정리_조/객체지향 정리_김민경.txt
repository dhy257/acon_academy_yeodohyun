객체지향프로그래밍 (구조화된 데이터 + 함수)
1. 캡슐화 (묶음, 데이터보호)
	관련있는 데이터와 데이터를 사용하는 함수(매서드)를 하나의 묶음으로 사용하는 것
	데이터를 외부에 직접 공개하지 않고 매서드를 통해서만 데이터를 사용하도록 하는 것
	접근제한자를 제공함 (private, public)


1. 데이터 + 데이터를 다루는 함수를 하나의 단위로 설계한다 
2. static을 제거한다 ( => 우리가 설계한 타입의 변수가 만들어진 다음부터 사용 가능하다) 
3. 구조화된 변수을 정보를 제공받는 매서드에서  변수의 정보를 제공받는 것을 생략된 형태로 사용할 수 있다
4. this를 키워드를 제공한다.

 //public static void input(Drink2 drink, String drinkName, int price, boolean isCode  ,String 칼로리){
    public  void input( String drinkName, int price, boolean isCode  ,String 칼로리){
        this.drinkName = drinkName;
        this.price = price;
        this.isCold = isCode;
        this.칼로리 = 칼로리; 
    }  

static 매서드는 객체 없이 호출가능, this를 사용할 수 없음. this는 객체를 의미하기 때문

따라서, 매서드를 static으로 쓸지 instance로 쓸지는 객체마다 변하는 값인지, 전체 class에서 하나로 작용하는 값인지를 확인 한 후에 판단하면 된다고 이해함


Private 요소의 접근방법
- 반드시 선언된 객체 내부에만 접근할 수 있다.
 
//성적처리하기
	private void calc() {
		int total = this.kor + this.eng;
		this.avg = total/2;
	}

getter(속성의 값을 반환) 
-	작성법 규칙 : get속성명  get+ 속성명 첫글자대문자 규칙 getName, getKor
setter(속성의 값을 변경) 
-	작성법 규칙 : set속성명  set+ 속성명 첫글지대문자 규칙 setName, setKor
-	setter는 변경이 필요한 속성에 한해서 만든다
	

