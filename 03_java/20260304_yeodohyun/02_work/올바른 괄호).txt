올바른 괄호)
괄호가 입력되면 올바른 괄호이면 “YES", 올바르지 않으면 ”NO"를 출력합니다.
(())() 이것은 괄호의 쌍이 올바르게 위치하는 거지만, (()()))은 올바른 괄호가 아니다.
단, 문자열의 최대 길이는 30

0) 괄호가 입력되면 >> 입력데이터 / 단 문자열의 최대 길이 30이라는 조건 지켜야함(if)
// 괄호가 올바른 조건이 무엇인지 파악 / 왼괄호가 오면 반드시 쌍으로 오른괄호로 닫혀야 한다
1) 올바른 괄호이면 >> 조건(if) / yes 출력
2) 올바르지 않으면 >> 조건(else) / no 출력
3) 조건에 따라 yes or no 출력 >> 출력 데이터

1. 입력 데이터
    - 변수명: str
    - 자료형: String
    - 최대 길이 30 제한

2. 내부 데이터
    - 변수명: stack
    - 자료형: Stack<Character>
    - ( 저장용

    - 변수명: x
    - 자료형: char
    - 문자열 저장용

3. 출력 데이터
    - 변수명: answer
    - 자료형: String
    - YES or NO


4. 처리
	0. 데이터 초기화 / Stack 생성 / Stack<Character> stack = new Stack<>();
	1. 예외 처리 / str 30초과시 NO / if (str.length() > 30) return "NO"; 
	2. 반복 시작 / 정해진 문자열 내에서 반복 >> 제한 존재 >> for문사용 / for(char x : str.toCharArray())
	







        - 1. '(' 이면 /  stack.push(x)
        - 2. ')' 이면:
            - 스택이 비어있으면: "NO" 반환
            - 스택에 값이 있으면: stack.pop()
    3. 최종 판단: 반복 종료 후 스택이 비어있지 않으면 "NO", 비어있으면 "YES" 반환
